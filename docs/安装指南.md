# 中文版矮人要塞 安装指南

> Dwarf Fortress **Steam 版 v53.06**（64 位）整合包
> 本包 = 游戏本体 + 免 Steam 运行 + 中文界面(dfint 汉化) + DFHack 53.06-r1 + DF-FanYi AI 动态汉化引擎桥
> 效果：界面、生物/物品名称与描述为中文；**公告、事件、战报等动态文本实时翻译成中文**显示在屏幕底部。

---

## 一、包内容

| 内容 | 说明 |
|---|---|
| `Dwarf Fortress.exe` | 游戏主程序（Steam 版，内置免 Steam 运行） |
| `hack/` + `dfhooks.dll` + `dfhooks_dfhack.dll` | DFHack 插件框架（53.06-r1） |
| `dfhooks_dfint_cjk.dll` + `dfint-data/` | dfint 中文汉化（界面/静态词典，Ctrl+F2 切换） |
| `dfhack-config/init/dfhack.init` | 已配好：进游戏自动开启公告捕获 + 中文字幕渲染 |
| `hack/scripts/fanyi.lua` + `hack/data/fanyi-font/` | DF-FanYi 引擎桥脚本 + CJK 字形(3877 个) |
| `DF-FanYi-离线翻译包/engine.db` | **离线翻译包**：5886 句预翻译 + 4708 条名称词条（无需 AI 也能命中大量中文） |
| `中文版矮人要塞安装指南.md` | 本文件 |

---

## 二、系统要求

- **Windows 10/11 64 位**，或 **Linux**（本指南主要面向 Linux + Proton 玩法）
- 内存 ≥ 8GB（16GB 更佳）；磁盘剩余 ≥ 2GB
- Linux 玩法需要：Steam + Proton 兼容层（推荐 **GE-Proton11-6** 或 Proton Experimental，教程见第四节）

---

## 三、解压（多卷加密压缩包）

收到的是分卷压缩文件（`.7z.001 / .7z.002 / ...`），需全部下载在同一文件夹：

- **Windows**：安装 [7-Zip](https://www.7-zip.org) → 右键第一个 `.7z.001` → 选择“解压到当前文件夹”（会提示输入密码）
- **Linux**：`7z x 文件名.7z.001`（按提示输密码）

解压后得到 `Dwarf Fortress` 文件夹。

> ⚠️ **必须解压到纯英文/数字路径**（如 `D:\Games\DF` 或 `~/Games/DF`）。
> 路径含中文/空格会导致游戏贴图加载失败，弹窗报 “Tileset not found”（实际是路径问题，别被报错误导）。

---

## 四、启动游戏

### Windows
双击 `Dwarf Fortress.exe` 即可 → DFHack 与汉化自动加载，进主菜单就是中文。

### Linux（Steam + Proton，推荐）
1. 安装 Steam，登录后开启 Proton：设置 → 兼容性 → 勾选“为所有其他产品启用 Steam Play”，兼容层选 Proton Experimental
2. Steam → 左下角 **添加游戏 → 添加非 Steam 游戏** → 浏览，选中 `Dwarf Fortress.exe`
3. 库中找到它 → 右键 → 属性 → 兼容性 → **强制使用特定的 Steam Play 兼容性工具**：选 **GE-Proton11-6**（未安装则先在兼容层文件夹或 ProtonUp-Qt 装好）或 Proton Experimental
4. 启动游戏

> 装完 DFHack 后首次启动会比平时慢（初始化前缀），耐心等 1–2 分钟。

---

## 五、中文界面

- 开箱即中文（dfint 汉化自动加载）
- **Ctrl+F2** 中英文切换
- 静态词典已覆盖：全部生物、植物、物品的名称与描述（本包自带社区齐全数据）

---

## 六、克隆翻译引擎（α 实时翻译的基础）

> 💡 **新机器推荐直接用一键脚本（第十一节），下面的手动方式二选一即可，两者等价。**

公告、事件、战报等动态文本不在静态词典里，需要 **DF-FanYi 引擎**实时翻译。

```bash
# 1. 克隆引擎（需要 Python ≥ 3.11，Arch 系自带；Ubuntu 需先装 python3.11+）
git clone https://github.com/stflj2022/DF-FanYi.git
cd DF-FanYi

# 2. 创建 Python 虚拟环境并安装
python3 -m venv .venv
source .venv/bin/activate
pip install -e .

# 3. 放置离线翻译包（本整合包附带，5 秒完成）
mkdir -p data
cp <你解压出的路径>/DF-FanYi-离线翻译包/engine.db data/

# 4. 启动引擎桥（保持此终端窗口开着；建议开机自启，见常见问题 6）
python -m df_fanyi bridge --transport tcp --port 17486
```

看到 `桥已启动: tcp 127.0.0.1:17486` 即成功。

---

## 七、配置 AI 模型（可选，强烈建议）

离线词典覆盖常见句子；未覆盖的动态文本由 AI 模型翻译。**二选一**：

### 方案 A：本地模型（免费、离线、隐私好；CPU 可跑但较慢）
```bash
# 安装 ollama：https://ollama.com（或 Linux: curl -fsSL https://ollama.com/install.sh | sh）
ollama pull gemma-4b-trans   # 约 5.3GB；CPU 也能跑（慢一些，但完全免费）
```
本项目 `config/default.yaml` 的 `local_llm.base_url` 默认就是 `http://127.0.0.1:11434`，无需改。

### 方案 B：云端模型（快，需 API Token）
- 启动 Pi Model Router（`model-router`，监听 127.0.0.1:8010），或
- 改 `config/default.yaml` 的 `providers` 加入任意 OpenAI 兼容端点；密钥放 `~/.config/df-fanyi/secrets.env`（**密钥绝不进 Git**）

### 不配 AI 会怎样？
引擎照常工作：能命中的（预翻 + 词典）出中文，未命中的安静显示原文。游戏永不卡顿、永不出错——但全中文覆盖率取决于你的模型配置。所以想完整中文体验就配一个。

---

## 八、全自动闭环（游戏里零操作）

配置好后一切自动，无需任何游戏内操作：

```
开机/会话启动 → 引擎桥常驻(127.0.0.1:17486)
   → 启动游戏 → DFHack 自动加载 → dfhack.init 自动 fanyi start
   → 公告/战报出现 → 引擎翻译 → 底部字幕条显示中文(3877 字形 CJK)
   → 引擎万一挂了 → 游戏照常显示原文，不卡不弹错，桥恢复后自动重连
```

---

## 九、验证是否生效

进游戏、进入世界后随便触发一条公告（如迁移者到来、贸易队到达），看屏幕底部是否出现中文字幕。

命令行验证（Linux）：
```bash
# 游戏运行时，在 DFHack 环境执行（或用 dfhack-run.exe "fanyi" "status"）
fanyi status
# 期望: 引擎: 在线 … 状态: running … 字幕: 开  CJK渲染: 就绪
```

---

## 常见问题（FAQ）

**1. 启动弹 “Tileset not found” / 画面花屏**
路径含中文/特殊字符 → 整个文件夹移到纯 ASCII 路径（见第三节）。

**2. 公告还是英文**
按顺序排查：
1) 引擎桥是否已启动（`python -m df_fanyi bridge ...`）？没起就起一个，再重进游戏（fanyi 会自动重连）。
2) 查看桥日志 `logs/engine.log`：若有 `LLM 调用失败, 回退原文` → 说明没配 AI 模型且该句不在离线词库，按第七节配一个模型。
3) 确认离线翻译包已复制（第六节第 3 步）。

**3. 字幕条没显示**
游戏内 DFHack 控制台执行：`fanyi overlays on cjk`

**4. 杀毒软件/防火墙报毒**
`steam_api64.dll`（Goldberg 免 Steam 模拟器）是常见误报源，无病毒。加入白名单即可。

**5. 存档在哪 / 换机迁移**
存档在游戏目录内 `data/save/` + `userdata/`，随文件夹整体走 → 整个 `Dwarf Fortress` 文件夹拷走即完成迁移（绿色便携）。

**6. Linux 开机自启引擎桥（systemd）**
```ini
# ~/.config/systemd/user/df-fanyi-bridge.service
[Unit]
Description=DF-FanYi translation bridge
After=network.target
[Service]
Type=simple
WorkingDirectory=<你的DF-FanYi克隆路径>
ExecStart=<你的python路径> -m df_fanyi bridge --transport tcp --port 17486
Restart=always
RestartSec=5
[Install]
WantedBy=default.target
```
```bash
systemctl --user daemon-reload
systemctl --user enable --now df-fanyi-bridge
```

**7. 需要 Steam 账号才能玩吗？**
不需要。本包内置免 Steam 运行（Goldberg 模拟器），联网与账号都非必需；Steam 只是 Linux 下跑 Proton 的载体。

---

## 十、卸载/清理

- 游戏：删除 `Dwarf Fortress` 文件夹即可（存档也在里面，删除前如需保留请先备份）
- 引擎：删掉克隆的 `DF-FanYi` 目录 + （若有）`systemctl --user disable --now df-fanyi-bridge`

## 十一、一键部署脚本（新 omarchy / Linux 机器，推荐）

整合包目录里自带 `install-df-fanyi-omarchy.sh`——在新机器上**一条命令**装好引擎桥
（+ 可选 AI 路由），并注册 systemd 开机自启。

### 1. 拷贝到新机器
把整个 `Dwarf Fortress` 文件夹拷到新机器（纯英文路径，见第三节），例如 `~/Games/`。

### 2. 跑一条命令
```bash
cd ~/Games/Dwarf\ Fortress
bash install-df-fanyi-omarchy.sh
```
脚本自动完成：克隆 DF-FanYi 引擎 → 建 venv → 复制离线翻译包 engine.db → 注册
并启动 `df-fanyi-bridge`（systemd user 服务，开机自启）。结束后启动游戏即为中文。

### 3. （可选）让“漏网长句”也实时中文 —— 云端 AI 路由
默认引擎**纯离线也能用**（静态词典 + 离线翻译包全覆盖大多数界面）；只有碰到词典
外的动态长句时，中文覆盖率才取决于是否配了 AI。想让公告/战报等彻底全中文：

- **方式 A（云端，快）**：从旧机器把 `model-router` 项目拷到游戏目录再重跑脚本：
  ```bash
  # 旧机器上（排除 env 密钥 / 日志 / 虚拟环境）
  rsync -a --exclude env --exclude logs --exclude .venv --exclude tracking.json \
        ~/model-router/  "<游戏目录>/model-router/"
  # 新机器游戏目录内重跑
  bash install-df-fanyi-omarchy.sh
  # 然后按提示把 API key 填进:  <游戏目录>/model-router/env
  #   GLM_API_KEY=你的智谱key   (或 SCNET_API_KEY / OPENROUTER_API_KEY)
  # 填完重跑脚本即自动启动路由; 之后游戏内漏网长句即实时中文化
  ```
- **方式 B（本地，免费离线）**：装 ollama（第七节方案 A），把 DF-FanYi
  `config/default.yaml` 的 `local_llm.base_url` 指向本地 ollama，重启引擎桥。

### 4. 验证
```bash
systemctl --user is-active df-fanyi-bridge   # → active 即引擎桥在跑
systemctl --user is-active df-fanyi-router    # → active 即 AI 路由在跑(配了才有)
```
进游戏后 DFHack 控制台 `fanyi status` 应显示引擎在线。

> 🔒 安全说明：脚本只写当前用户目录（`~/.config/systemd/user` 等），**不需要 sudo**；
> API key 只存本地 `model-router/env`，绝不进脚本或 git 仓库。

---

*整合包版本：DF v53.06 (Steam) · DFHack 53.06-r1 · dfint-lite 20251127 · DF-FanYi 引擎（Stable）*
*引擎仓库：https://github.com/stflj2022/DF-FanYi（问题请开 Issue，注明游戏/系统版本）*